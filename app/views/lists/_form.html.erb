<%= simple_form_for(list) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit list.new_record? == false ? "Edit this list" : "Add new list" , class: "btn btn-primary" %>
<% end %>
